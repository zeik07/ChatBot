@{
    ViewData["Title"] = "Commands";
}

<h2>Commands</h2>
<div>
    <h4>List of all current commands.</h4>
    @foreach (KeyValuePair<string, string> command in CommandList.CommandsList)
    {
        <div>
            <label>!@command.Key.ToString()</label>
        </div>
    }
</div>